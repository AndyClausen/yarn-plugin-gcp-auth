{
  "name": "yarn-plugin-gcp-auth",
  "version": "1.3.1",
  "main": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "builder build plugin",
    "local-install": "yarn plugin import ./bundles/@yarnpkg/plugin-gcp-auth.js",
    "test": "yarn build && yarn local-install && yarn"
  },
  "dependencies": {
    "@types/node": "^17.0.27",
    "@yarnpkg/builder": "~3.2.0",
    "@yarnpkg/cli": "~3.2.0",
    "@yarnpkg/core": "~3.2.0",
    "@yarnpkg/plugin-npm": "~2.7.0"
  },
  "devDependencies": {
    "prettier": "~2.6.2",
    "typescript": "^4.6.3"
  },
  "packageManager": "yarn@3.2.0"
}
